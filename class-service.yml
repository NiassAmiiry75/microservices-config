server:
  port: 8082

spring:
  application:
    name: class-service

  datasource:
    url: jdbc:mysql://localhost:3306/amiiry
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
